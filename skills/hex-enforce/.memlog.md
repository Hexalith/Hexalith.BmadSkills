---
skill: hex-enforce
intent: build
updated: 2026-07-13T16:54
---

- (event) Build started from module plan skills/reports/module-plan-bmad-hexalith.md, roadmap step 3: audit core + hex-enforce; siblings hex-absorb (KB writer, complete) and hex-consult (built, baseline eval pending re-run after auth fix) define the KB contract enforce reads
- (decision) Audit core home: hex-enforce/references/audit-core.md — standalone reference, parameterized by scope (diff|repo) and layer (judgment-only|full); consumers: enforce (diff, now), hex-migrate (repo-wide) and fleet aggregator (later; file-load vs headless-invoke settled when migrate is built)
- (decision) All four implied behaviors accepted: (1) LLM layer reviews mechanical rules the analyzer doesn't cover, verdict names the gap; (2) expired waivers stop suppressing and surface as findings; (3) verdict/findings JSON schema owned by audit core — migrate's gap report = same schema + tiers; (4) analyzer coverage read from a rules manifest (diagnostic id → convention id) shipped by Hexalith.Builds, absent = zero coverage
- (decision) customize.toml: NO — ships fixed, matches hex-absorb and hex-consult; module config is CM/setup's job
- (decision) Waivers: hex-waivers.yaml at target repo root, constrained flat format (id, reason, review date per entry), parsed+validated by scripts/gate.py; template in assets/
- (decision) scripts/gate.py, one stdlib-only script (kb.py pattern), subcommands: waivers (parse+validate+expiry), coverage (KB mechanical ids × manifest), staleness (meta.md targets_framework vs repo Directory.Packages.props), verdict (validate findings JSON, apply waivers, exit code, gh annotations). git-diff scoping deliberately NOT scripted — the model runs git diff itself
- (direction) hex-consult baseline eval re-run scheduled after this build completes (auth fixed via adapter.json)
- (event) Built: SKILL.md (1214 tokens), references/audit-core.md (1350 tokens — shared audit engine: procedure, findings JSON schema, waiver semantics, staleness; scope diff|repo × layer judgment|full), scripts/gate.py (waivers/coverage/staleness/verdict, stdlib-only) + 12 passing unit tests, assets/hex-waivers-template.yaml, evals/cases.json (3 cases: gate-fails-cited, waiver-respected-expiry-bites, coverage-map) with kb-mini + repo-mini fixtures validated+indexed via hex-absorb kb.py
- (note) Lint: quick_validate ok; scan-scripts 0 findings; scan-path-standards flags .memlog.md at root (high) — false positive per builder convention, hex-absorb and hex-consult ship identically. Smoke run of all four gate.py subcommands on the real fixtures produced the exact expected partitions (coverage covered/uncovered/unknown, staleness quiet at 1.72.0, waiver active/expired split)
- (direction) User chose both post-build gates: full Analyze validation (run first, findings triaged/fixed), then baseline eval run via bmad-eval-runner on the hardened skill; hex-consult baseline re-run already running in background
- (event) analyze: grade good, 0 critical / 1 high / 8 medium / 2 low, report .analysis/2026-07-13-1643/skill-analysis-report.html
- (event) Fix pass: 10 of 11 analyze findings applied — gate.py verdict rejects convention ids absent from the KB (determinism-1), --manifest moves judgment-layer findings on covered ids to surfaced analyzer_owned bucket (determinism-3), new rules subcommand with index_drift pre-pass (determinism-2), headless return gains artifact channel + inline gap content for ephemeral checkouts (architecture-1/enhancement-2/enhancement-3), script-unavailable now blocks headless with by-hand fallback scoped interactive-only (architecture-2), repo-scope redirect to hex-migrate added (enhancement-1), language variables in audit-core.md (architecture-4), intake frontmatter dedup (leanness-1). 17/17 unit tests pass, quick_validate ok, scan-scripts clean, SKILL.md 1380 tokens, audit-core.md 1495
- (decision) Module convention recorded (resolves analyze architecture-3, applies to hex-absorb/hex-consult/hex-enforce uniformly): hex-* skills are stateless transactional workflows; headless runs carry their assumptions (scope, layer, target, citations) inside their returned JSON payloads, not per-run memlogs, and returns carry no memlog path — a deliberate exemption from the builder principles' headless-memlog bar
