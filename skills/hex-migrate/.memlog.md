---
skill: hex-migrate
intent: build
updated: 2026-07-13T17:23
---

- (event) Build started from module plan skills/reports/module-plan-bmad-hexalith.md, roadmap step 4 (hex-migrate, the beating heart); invoked as 'create missing skill' — roadmap order picked hex-migrate over hex-extend/hex-create since absorb/consult/enforce are complete
- (decision) Audit-core consumption settled (deferred from hex-enforce build): FILE-LOAD — migrate loads references/audit-core.md and runs scripts/gate.py from the sibling hex-enforce skill directory; headless-invoke rejected because enforce's headless surface is deliberately diff-scoped and redirects repo work here. Explicit path in invocation overrides (also the eval-fixture path); hex-enforce absent with no override = blocked
- (decision) Three intents, not four: plan-migration folds into drive (audit-then-drive UX from ideation; a drive without an approved plan starts by planning). Intents: audit, drive, fleet — 3 CSV entries at packaging
- (decision) Tier semantics: judgment-tagged KB rules are ALWAYS judgment tier (machine never settles a human call); mechanical-tagged rules split mechanical (safe local deterministic fix) vs structural (reshaping across files/projects, needs approved plan) by blast radius — model judgment at report time, validated deterministically by migrate.py tiers against gate.py rules output
- (decision) scripts/migrate.py stdlib-only (gate.py/kb.py pattern), subcommands: tiers (validate tier field vs KB tags), report (settled tiered verdict -> gap report HTML + JSON), fleet (dir of settled verdicts -> fleet HTML + JSON with lag, drift hot-spots, waiver counts). Green gate and git discipline deliberately NOT scripted — model runs dotnet/git itself, matching gate.py's unscripted git diff
- (decision) customize.toml: NO — matches the recorded module standard (absorb/consult/enforce all ship fixed; module config is CM/setup's job)
- (decision) Module conventions inherited: stateless transactional workflow, no per-run memlog, headless assumptions ride the returned JSON; migration state lives in the target repo's git history (one step = one commit on a hex-migrate/<slug> branch) plus gap report + migration log in {output_folder}/hex/
- (event) Built: SKILL.md (1579 tokens, 3 intents: audit/drive/fleet), scripts/migrate.py (tiers/report/fleet, stdlib-only) + 15 passing unit tests, evals/cases.json (3 cases: audit-tiered-gap-report, drive-mechanical-only-headless, fleet-aggregation) with fixtures: kb-mini (4 entries incl. superseded aggregate-mutable-apply -> aggregate-pure-functions from-state pair, validated+indexed via hex-absorb kb.py), repo-lagging (buildable offline — dotnet build green, Hexalith.Commons stub ships in-fixture so the mechanical fix compiles), hex-enforce-skill (audit-core.md + gate.py copies for the sandbox where the sibling is not installed), fleet-verdicts (2 settled tiered verdicts)
- (note) Smoke run end-to-end on real fixtures: gate.py rules (4 entries, no drift) -> hand-authored findings -> gate.py verdict (fail, tier fields preserved through settling, staleness repo_lags_target=true) -> migrate.py tiers (ok, 1/1/1) -> report (HTML+JSON pair) -> fleet (2 repos, hot-spot ulid-identifiers x2, 1 waived)
- (note) Lint: quick_validate ok; scan-scripts 0 findings; scan-path-standards flags .memlog.md at root (high) — known false positive per builder convention, all three siblings ship identically. SKILL.md 1579 tokens, under the 2000 desired tier
- (assumption) Drive baseline gate: an already-red baseline blocks drive (green gate cannot tell migration breakage from pre-existing breakage) — implied by never-leave-red, not explicit in the module plan
- (assumption) Fleet intent accepts pre-settled verdicts (aggregate-only invocation) in addition to the full audit-each-repo path — needed for cheap re-aggregation and the eval; the manifest-driven path stays the default
