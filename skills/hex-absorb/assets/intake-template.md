---
source: consult
filed: 2026-01-01
status: open
---

# One-line question or finding

Context: what was asked or found, where, and why the KB could not answer it.
Filed by hex-consult (unanswerable question), hex-enforce (undecidable judgment
call), hex-migrate, hex-extend, or hex-create (KB silent on something a run
needed), a human, or hex-absorb itself (out-of-scope discovery mid-run).
hex-absorb's intake intent resolves it into a KB entry — `source` is one of
consult | enforce | migrate | extend | create | human | absorb, `status` flips to
resolved (or the file is removed) in the same reviewed diff that adds the entry.
