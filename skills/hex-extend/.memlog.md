---
skill: hex-extend
intent: build
updated: 2026-07-13T17:38
---

- (event) Session start: user asked to 'create missing skill'; roster check found hex-extend (roadmap #5) and hex-create (#6, built last by design) missing — target: hex-extend per module plan spec
- (decision) Routes carry no convention content — conventions live only in the KB (meta-drift countermeasure); route lines name the piece and outcome, KB entries supply patterns/placement/naming per route
- (decision) Verify gate = sibling hex-enforce audit core, diff scope, judgment layer, settled via gate.py verdict — exactly the call hex-enforce's SKILL.md already promises for hex-extend
- (decision) Green-baseline before generating (mirrors hex-migrate): a red repo makes the gate unable to tell new breakage from pre-existing; interactive surfaces it, headless blocks
- (decision) No custom script: deterministic work is owned by dotnet (build/test), sibling gate.py (verdict), git — nothing extend-specific passes the determinism test; declined, revisit if eval transcripts show repeated helper-writing
- (decision) No working-state memlog in the built skill: module plan mandates stateless workflows; one extension is one session and state lives in the working-tree diff
- (assumption) customize.toml declined by default, matching all four siblings (none ship one); autonomous session — surfaced at handoff for reversal
- (decision) Evals ship with the skill (module-plan rule): 3 headless cases — add-command happy path, ambiguous-route blocked, tests-omission pressure test; fixtures clone hex-migrate's approach (stub-compilable repo, kb-mini, vendored audit core)
- (event) Fixtures verified: repo-current builds green with dotnet 10.0.301 (offline stubs), dotnet test exits 0 on the stub test project, kb.py validate clean on 6 entries, index.md generated by kb.py
- (event) Ship gates: SKILL.md 1233 tokens (desired 2000); quick_validate ok; scan-scripts pass; scan-path-standards 1 high (.memlog.md at root) — known scanner/process conflict, identical finding on validated sibling hex-migrate; resolved at CM packaging (dot-files excluded), not by moving the memlog
- (note) Deliberately absent, per plan: no UI-page fixture in evals (route exists, KB will carry Blazor/FrontComposer entries when absorbed); split the UI route to its own skill only if it grows fat
- (event) Build complete; pending: user memlog audit, optional full Analyze run, optional eval-runner baseline/trigger runs
