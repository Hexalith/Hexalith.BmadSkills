---
skill: hex-consult
intent: build
updated: 2026-07-13T16:54
---

- (event) Build started from module plan skills/reports/module-plan-bmad-hexalith.md (self-contained brief, roadmap step 2); sibling hex-absorb defines KB contract consult reads
- (decision) Name hex-consult per plan; workflow skill, no persona/memory; intents: answer question, show example, explain finding, file KB gap; interactive+headless
- (note) KB contract from hex-absorb: root {project-root}/_bmad/hex/knowledge/, entries/<id>.md with tag/since/until/verified/provenance, generated index.md, intake/*.md with source=consult; consult is entries-reader, intake-writer
- (decision) User accepted all four implied behaviors: version-aware answers (since/until + repo framework version), trust signals in citations (verified date, flag unverified/stale), source-fallback answering (answer from framework source + file gap same turn), auto-file gaps (no per-filing confirmation)
- (decision) customize.toml: NO — ships fixed, matches hex-absorb; verbosity/framework source come from module config
- (decision) Evals: cases.json + fixture KB + baseline run via bmad-eval-runner (plan makes evals a release gate)
- (event) SKILL.md drafted (single-file, no scripts/references; assets dir removed); eval suite: 4 cases (answer-cited, version-aware-answer, source-fallback-gap, unanswerable-no-hallucination) with kb-mini fixture validated+indexed via hex-absorb kb.py
- (decision) Lint: quick_validate ok, scripts n/a, tokens 1300/2000. Path scanner 'memlog at root' finding treated as false positive — builder convention mandates {target-skill-path}/.memlog.md for resume; hex-absorb ships identically
- (event) Baseline eval run 20260713-153242 errored: all 8 invocations 'Not logged in' — clean-room env forwards only ANTHROPIC_API_KEY (unset on host); host session is OAuth-authenticated which the isolation contract deliberately does not stage
- (decision) Eval auth: OAuth token via claude setup-token; evals/adapter.json written with auth_env CLAUDE_CODE_OAUTH_TOKEN (picked up automatically beside cases.json for all future runs)
- (note) Baseline re-run attempt 2026-07-13: still blocked — CLAUDE_CODE_OAUTH_TOKEN is unset on the host (adapter.json is correct; the clean-room forwards the var only when set). Fix: user runs 'claude setup-token' and exports CLAUDE_CODE_OAUTH_TOKEN, then re-run; no adapter change needed
- (note) Module convention (recorded 2026-07-13 during hex-enforce analyze): hex-* headless runs carry assumptions in their returned JSON payloads, not per-run memlogs — deliberate module-wide exemption from the builder principles' headless-memlog bar; applies uniformly to hex-absorb/hex-consult/hex-enforce
